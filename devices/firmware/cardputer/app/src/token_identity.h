#pragma once

#include "../../../common/token_identity.h"

namespace standalone {
using anchor_identity::ADDRESS_MAX;
using anchor_identity::CHAIN_MAX;
using anchor_identity::sameAddress;
using anchor_identity::sameTokenIdentity;
namespace identity = anchor_identity::identity;
}  // namespace standalone
