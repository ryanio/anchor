{
  "name": "@anchor/widget",
  "version": "0.0.1",
  "private": true,
  "description": "Anchor's Quickshell top-bar widget. Reads the local data service; never signs anything.",
  "//": "commonjs, unlike every other workspace here: PulseModel.js is loaded by QML's script engine as a plain script, so its exports have to be reachable through require() rather than through an ESM import. Only the tests are ESM.",
  "type": "commonjs",
  "engines": {
    "node": ">=26.0.0"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "license": "MIT"
}
