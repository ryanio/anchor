# secrets — never commit
.env
.env.*
*.key
*.pem
secrets.*

# local state / cache
.cache/
data/
*.sqlite
*.sqlite3
*.db

# node
node_modules/
dist/
build/
*.tsbuildinfo

# python
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.ruff_cache/

# packaging artifacts
packaging/*.pkg.tar.zst
packaging/src/
packaging/pkg/

# os / editor
.DS_Store
*.swp
.idea/
.vscode/

# firmware build output — PlatformIO; arduino-cli builds into ~/.cache/arduino
.pio/
# the simulator's NVS, written beside the project it was run from
sim-nvs-*.txt

# site build output
site/dist/

# Agent worktrees live here; they are checkouts, never content.
.claude/worktrees/

# Screenshot review output — regenerate with `node scripts/review.ts`
review/
